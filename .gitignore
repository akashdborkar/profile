# Claude Code session data
.claude/

# Node
node_modules/
.next/
build/

# cms/dist/ is intentionally tracked — pre-built Strapi admin for Render free-tier
# (Render 512 MB RAM cannot run the Vite admin bundle build)
# Exclude local dev uploads that Strapi writes to disk (production uses Cloudinary)
cms/dist/build/uploads/*
!cms/dist/build/uploads/.gitkeep
profile/dist/

# Environment
.env
.env.local
.env*.local

# OS
.DS_Store
Thumbs.db

# Vercel CLI project link (auto-generated)
.vercel

# Screenshots and debug images generated during development sessions
/*.png
/*.jpg
/*.jpeg

# Playwright MCP artifacts
.playwright-mcp/
