{
  "collectionName": "components_shared_curated_items",
  "info": {
    "displayName": "Curated Item",
    "icon": "star",
    "description": "A manually curated featured content reference"
  },
  "options": {},
  "attributes": {
    "contentType": {
      "type": "enumeration",
      "enum": ["Blogs", "Projects", "Engagements"],
      "required": true
    },
    "targetId": {
      "type": "integer",
      "required": true
    }
  }
}
